1.理解HTML页面结构 （行集合 / 带节点的树形结构 ）

2.用三种方法解析页面
a.逐行方法（正则表达式的HTML分折技术）
b.树形结构方法（Python的BeautifulSoup库和一个Scraper的基于chrome浏览器的工具）



1.行分隔模型

*必须把网页中的文字和嵌套在其中的HTML元素本身当作分隔符。
可以使用<h2></h2>标签作为分隔符提取聊天日志中的日期
<li></li>作为分隔符找到其中rel="提取聊天人的名字"


2.

